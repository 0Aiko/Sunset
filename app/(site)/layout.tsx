import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "welcome | itai.su",
  openGraph: {
    title: "welcome | itai.su",
  },
};

export default Page;
