import { Metadata } from "next";
import Page from "./page"; 

export const metadata: Metadata = {
  title: "Settings | itai.su",
  openGraph: {
    title: "Settings | itai.su",
  },
};

export default Page;
