import { Metadata } from "next";
import Page from "./page"; 

export const metadata: Metadata = {
  title: "Top plays | itai.su",
  openGraph: {
    title: "Top plays | itai.su",
  },
};

export default Page;
